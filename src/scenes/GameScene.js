export default class GameScene extends Phaser.Scene {
  constructor() {
    super({ key: 'GameScene' });
  }

  preload() {
    // Load assets here
  }

  create() {
    // Create game objects here
  }

  update(time, delta) {
    // Update game state here
  }
}

